import { AppRupiahDirective } from './app-rupiah.directive';

describe('AppRupiahDirective', () => {
  it('should create an instance', () => {
    const directive = new AppRupiahDirective();
    expect(directive).toBeTruthy();
  });
});
